from .selector import EasyPromptSelector

NODE_CLASS_MAPPINGS = {
    "EasyPromptSelector": EasyPromptSelector,
}

NODE_DISPLAY_NAME_MAPPINGS = {
    "EasyPromptSelector": "Easy Prompt Selector (Bangs)",
}
